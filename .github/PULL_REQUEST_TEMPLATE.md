## What changed

Describe the changes in this PR. Be specific — link to issues, files, or
sections touched.

## Why

Explain the motivation. If this is a bug fix, describe the broken behavior.
If it is a feature, describe the user or developer problem it solves.

## How verified

Reference concrete checks you ran. Example:

- `pnpm build` exits 0
- `grep -n "something" src/path/file.tsx` matches / returns no output
- Manual: tabbed through the command palette and confirmed focus returns to
  the ⌘K trigger on Esc.

## Screenshots

For UI changes, include before/after screenshots or a short screen recording.
For non-UI changes, delete this section or write "N/A".
